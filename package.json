{
  "name": "@v4in-glory/mcp-test",
  "version": "0.1.0",
  "type": "module",
  "bin": { "vmcp": "server/dist/index.js" },
  "scripts": {
    "prepare": "npm --prefix server ci --ignore-scripts && npm --prefix server run build && npm --prefix server run build:plugin"
  }
}
