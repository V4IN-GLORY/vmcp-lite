{
  "name": "@v4in-glory/mcp-test",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "vmcp": "server/dist/index.js"
  },
  "scripts": {
    "prepare": "tsc -p server && rojo build plugin.project.json -o VMCP.rbxmx && npm --prefix openpencil-headless install --no-audit --no-fund"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "ws": "^8.18.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13",
    "typescript": "^5.7.2"
  }
}
